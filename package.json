{
  "name": "carrier-pigeon",
  "version": "0.1.10",
  "description": "command line parser that has no dependencies",
  "keywords": [
    "command",
    "line",
    "interface",
    "secure",
    "cli"
  ],
  "main": "index.js",
  "scripts": {
    "mocha": "ts-mocha -w -p tsconfig.json src/**/*.ts",
    "test": "mocha -w dist/test",
    "build": "tsc --project tsconfig.json --incremental false",
    "ts": "tsc --watch --project tsconfig.json --incremental false"
  },
  "author": "Scott Ballantyne",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.45",
    "mocha": "^9.2.0",
    "ts-mocha": "^9.0.2",
    "typescript": "^4.5.5"
  }
}
